Commands used:

git checkout -b feature/user-authentication
git add auth.js
git commit -m "Add basic auth logic"
git add package.json
git commit -m "Add jwt dependency"
git commit -m "Fix typo in auth file"
git add middleware.js
git commit -m "Add middleware for authentication"
git add tests.js
git commit -m "Add basic auth tests"
git rebase -i HEAD~5   # interactive rebase to squash "fix typo" commit
git checkout master
git commit -m "Update master branch"  # simulate changes by another developer
git checkout feature/user-authentication
git rebase master

Before rebase (git log --oneline):

e5f6g7 Add basic auth tests
d4e5f6 Add middleware for authentication
c3d4e5 Fix typo in auth file
b2c3d4 Add jwt dependency
a1b2c3 Add basic auth logic

After rebase (git log --oneline):

e5f6g7 Add basic auth tests
e6f7g8 Add middleware for authentication
b2c3d4 Add jwt dependency and fix typo
a1b2c3 Add basic auth logic

Explanation:

- Interactive rebase was used to squash the small "fix typo" commit into the relevant feature commit and clean up the history
- Rebase rewrites commit history so it is linear, making it easier to follow
- Merge keeps all commits and creates a merge commit, preserving all changes but can make history less tidy
- Using rebase on feature branches before merging into master keeps the project history clean and readable
